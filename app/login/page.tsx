import { LoginCard } from '@/components/login-card';

export const metadata = {
  title: 'Login - WasteSeg',
  description: 'Admin login for waste segregation system',
};

export default function LoginPage() {
  return <LoginCard />;
}
